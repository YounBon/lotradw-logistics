# Local .env example for LoTraDW backend
# Copy this file to `.env` (or `.env.local`) and fill in real secrets locally.
# DO NOT commit your real `.env` to git.

# Database (used by backend TypeORM config)
DB_HOST=::1
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=YOUR_DB_PASSWORD_HERE
DB_NAME=lotradwDB

# JWT
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=24h

# App
NEXT_PUBLIC_API_URL=http://localhost:3001/api
NODE_ENV=development
